# Battleship
Online multiplayer battleship game.

To setup the tunnel, run the startup server while ssh'ed to the PI. The script is `startup-tunnel-script.sh`. 
Note that you need to `chmod +x startup-tunnel-script.sh` to allow it to run as a executable. The tunnel should run. It is available at nginxpi.michaelwiciak.com


